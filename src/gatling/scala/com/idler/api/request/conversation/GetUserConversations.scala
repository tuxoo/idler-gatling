package com.idler.api.request.conversation

import io.gatling.core.Predef.exec
import io.gatling.core.structure.ChainBuilder

object GetUserConversations {
  private[idler] val getUserConversations: ChainBuilder =
    exec(

    )
}
