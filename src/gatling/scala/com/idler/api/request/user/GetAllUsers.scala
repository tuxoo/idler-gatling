package com.idler.api.request.user

import io.gatling.core.Predef.exec
import io.gatling.core.structure.ChainBuilder

object GetAllUsers {
  private[idler] val getAllUsers: ChainBuilder =
    exec(

    )
}
